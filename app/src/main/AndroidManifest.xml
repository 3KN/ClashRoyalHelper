<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.alex.smallapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".NewDeck" />
        <activity android:name=".First">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".wikiScroll"
            android:label="@string/title_activity_wiki_scroll"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".strategies"
            android:label="@string/title_activity_strategies"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".arenas"
            android:label="@string/title_activity_arenas"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>